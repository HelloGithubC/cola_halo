// Role in refactor stage:
// This header will define the C++-side configuration view for simulation
// parameters. It is intended to organize validated settings, safer string
// ownership, and grouped output options while remaining compatible with the
// existing C parameter-reading path during early migration.
