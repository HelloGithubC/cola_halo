// Role in refactor stage:
// This header will define the top-level C++ application orchestration layer.
// It is expected to own or coordinate the startup sequence, module wiring,
// realization loop, timestep driving, and high-level execution flow while
// continuing to call the existing C numerical kernels through a bridge layer.
